export type Numbers = 0 | 1;

export interface UserNumberType {
  firstName?: Numbers;
  lastName?: Numbers;
  mobile?: Numbers;
  email?: Numbers;
  password?: Numbers;
  otp?: Numbers;
  accessToken?: Numbers;
  refreshToken?: Numbers;
  role?: Numbers;
  avatar?: Numbers;
  nationality?: Numbers;
  gender?: Numbers;
  nationalId?: Numbers;
  province?: Numbers;
  city?: Numbers;
  postalCode?: Numbers;
  address?: Numbers;
  birthdate?: Numbers;
  createdAt?: Numbers;
  updatedAt?: Numbers;
}
